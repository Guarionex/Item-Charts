apiVersion: v1
kind: Service
metadata:
  name: item-charts-backend
  namespace: item-charts
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: item-charts-backend
